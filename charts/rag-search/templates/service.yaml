apiVersion: v1
kind: Service
metadata:
  name: rag-search-service
  labels:
    app: rag-search
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8010
      protocol: TCP
      name: grpc-http
  selector:
    app: rag-search
